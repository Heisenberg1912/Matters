# Database
MONGODB_URI=mongodb://localhost:27017/matters
NODE_ENV=development

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173,http://localhost:3000
CLIENT_ORIGIN=http://localhost:5173

# API Configuration
API_PREFIX=/api
PORT=4000

# Pusher (Real-time Notifications) - OPTIONAL
# Sign up at https://pusher.com for free tier
PUSHER_ENABLED=true
PUSHER_APP_ID=your_pusher_app_id
PUSHER_KEY=your_pusher_key
PUSHER_SECRET=your_pusher_secret
PUSHER_CLUSTER=mt1

# Email Configuration (for password reset, notifications, etc.)
# SMTP Settings - Gmail example (for other providers, update host/port)
EMAIL_SERVICE=gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=arnav@builtattic.com
EMAIL_PASS="doda ykmm ulca fbkz"
EMAIL_FROM="Builtattic <sup@builtattic.com>"
SUPPORT_INBOX=tushar@builtattic.com
SUPPORT_WEBHOOK_SECRET=dev_support_webhook_secret
EMAIL_FROM_NAME=Matters - Construction Management

# Frontend URL (for password reset links in emails)
FRONTEND_URL=http://localhost:5173

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=uploads

# Payment Integration (Optional - Razorpay)
RAZORPAY_ENABLED=false
RAZORPAY_KEY_ID=your_razorpay_key
RAZORPAY_KEY_SECRET=your_razorpay_secret

# Google Drive Integration (Optional)
GOOGLE_OAUTH_CLIENT_ID=your_client_id
GOOGLE_OAUTH_CLIENT_SECRET=your_client_secret
GOOGLE_OAUTH_REFRESH_TOKEN=your_refresh_token
GOOGLE_DRIVE_FOLDER_ID=your_folder_id

# AI/ML Features (Optional)
GEMINI_ENABLED=false
GEMINI_API_KEY=your_gemini_api_key

# Weather API (Optional)
OPENWEATHER_API_KEY=your_openweather_api_key
